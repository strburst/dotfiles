;;; which-key-use.el --- Popup menu showing available keybindings
;;; Commentary:
;;; Code:

(use-package which-key
  :config
  (which-key-mode 1))

(provide 'which-key-use)

;;; which-key-use.el ends here
