;;; uniquify.el --- Choose a buffer name disambiguation scheme
;;; Commentary:
;;; Code:

(require 'uniquify)
(setq uniquify-buffer-name-style 'forward)

(provide 'uniquify-cfg)

;;; uniquify-cfg.el ends here
