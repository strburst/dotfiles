;;; yasnippet-cfg.el --- Snippets engine, similar to Skeleton.el
;;; Commentary:
;;; Code:

(use-package yasnippet
  :config
  (yas-global-mode 1))

(provide 'yasnippet-cfg)

;;; yasnippet-cfg.el ends here
