;;; which-key-cfg.el --- Popup menu showing available keybindings
;;; Commentary:
;;; Code:

(use-package which-key
  :config
  (which-key-mode 1)
  :diminish which-key-mode)

(provide 'which-key-cfg)

;;; which-key-cfg.el ends here
